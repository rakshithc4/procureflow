projection;
strict ( 2 );

define behavior for ZC_PURCHASE_REQ alias PurchaseRequisition
{
  use create;
  use update;
  use delete;

  use action SubmitForApproval;
  use action Approve;
  use action Reject;
  use action CreatePurchaseOrder;

  use association _Order { create; }
}
